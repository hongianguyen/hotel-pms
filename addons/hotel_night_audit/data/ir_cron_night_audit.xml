<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data noupdate="1">
        <!-- Night Audit Cron Job — 02:00 AM Daily, AUTO ONLY -->
        <record id="ir_cron_hotel_night_audit" model="ir.cron">
            <field name="name">Hotel: Night Audit (02:00 AM)</field>
            <field name="model_id" ref="model_hotel_night_audit"/>
            <field name="state">code</field>
            <field name="code">model._cron_run_night_audit()</field>
            <field name="interval_number">1</field>
            <field name="interval_type">days</field>
            <field name="numbercall">-1</field>
            <field name="nextcall" eval="(DateTime.now().replace(hour=2, minute=0, second=0) + timedelta(days=1)).strftime('%Y-%m-%d %H:%M:%S')"/>
            <field name="active">True</field>
            <field name="user_id" ref="base.user_admin"/>
        </record>
    </data>
</odoo>
